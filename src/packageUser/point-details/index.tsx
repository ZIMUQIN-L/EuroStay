import { View, Text } from '@tarojs/components';
import { observer } from 'mobx-react';

const Index = () => {
  return (
    <View>
      <Text>积分明细</Text>
    </View>
  );
};

export default observer(Index);
